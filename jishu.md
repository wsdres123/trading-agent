

api 2次优化

一、数据层：从“能用”到“毫秒级精准”
你用了Redis做缓存，还做了Parquet+Msgpack+gzip的序列化优化，这个思路很好。但前排软件在数据链路上追求的是确定性低延迟和数据一致性。

分层缓存策略升级（L1内存 + L2 Redis）

现状：你的data.py是“Redis优先→内存兜底”，Redis挂了才用内存。

优化建议：采用L1内存缓存 + L2 Redis缓存的双层架构。对于spot=60s这类秒级热数据，第一次查询走Redis拿到后，直接在进程内存（L1）里也存一份d，TTL设更短（如10秒）。后续请求直接命中内存，连Redis的网络IO都省了，响应时间从几毫秒降到微秒级。这在高频刷新的Streamlit页面上效果会非常明显。

参考实现：可以用cachetools或自定义CacheManager实现L1，L2仍用Redis。

引入专业时序数据库，分担Redis压力

现状：所有行情快照和日K都压在Redis里，键格式用了sha256，查询灵活性有限。

优化建议：对于历史K线、日K、板块指数这类量大且需要复杂查询（如区间统计、多周期聚合）的数据，从Redis迁出，交给专业的时序数据库（如DolphinDB、InfluxDB）或列式存储（如ClickHouse）。

好处：专业时序库支持向量化计算，做“过去20日均线”“板块资金流向”这种计算比在Python里用Pandas快得多，而且能直接对接到你的LLM工具调用里。Redis则专注于存储最新的实时快照和会话状态，各司其职。

WebSocket行情直连与本地持久化

现状：从架构图看，数据源主要是通过HTTP轮询（qt.gtimg.cn等）获取。

优化建议：前排软件都使用WebSocket长连接接收L2行情推送，而不是轮询。你的架构中虽然有/ws/market，但那是FastAPI对外提供的，建议后端服务自身也通过WebSocket与数据源（如东方财富、同花顺的L2接口）建立持久连接。这样能：

将最新TICK数据实时推送到Redis，替代轮询的spot=60s，延迟从秒级降到毫秒级。

在本地（或挂载的SSD）持久化一份行情快照，用于盘后回测和策略复盘，避免每次都去拉取历史数据。


模型性能2次优化
构建”分层记忆”系统

现状：LLM 的上下文主要依靠 docs/*.md 和 knowledge/*.csv，以及每次对话的上下文（session history）。每次对话重新冷启动，AI 不记得上次分析了什么，无法积累交易经验。

优化目标：参考 FinMem 架构，为 AI 构建三层分层记忆，让它随着使用越来越”懂”你的交易风格。

---

一、三层记忆架构

┌─────────────────────────────────────────────────────────┐
│                    分层记忆 (Layered Memory)              │
├──────────────────┬──────────────────────────────────────┤
│  短期记忆 (STM)   │  当前会话对话上下文                   │
│  Session Memory  │  存储位置: Streamlit session_state    │
│                  │  生命周期: 随会话消亡（关闭浏览器）     │
│                  │  已实现: _build_messages(history)     │
├──────────────────┼──────────────────────────────────────┤
│  中期记忆 (MTM)   │  每日 AI 判断 + 市场复盘结果          │
│  Daily Memory    │  存储位置: .data/*_ai.json            │
│                  │  生命周期: 永久（每日追加，不覆盖）     │
│                  │  检索: RAG 向量检索，供策略反思        │
├──────────────────┼──────────────────────────────────────┤
│  长期记忆 (LTM)   │  成功/失败交易案例 + 市场规律总结     │
│  Experience Base │  存储位置: .knowledge_index/ (向量库) │
│                  │  生命周期: 永久（手动或定期整理）       │
│                  │  检索: BM25+向量 RRF 混合检索         │
└──────────────────┴──────────────────────────────────────┘

---

二、中期记忆（MTM）具体实现

存储 schema（.data/memory_log.jsonl，追加写入）：

  {
    “date”: “2026-08-03”,
    “type”: “emotion”,          # emotion / timing / theme / shortterm / trade
    “summary”: “今日情绪节点：修复--中等，大面 8 个，微盘强”,
    “ai_result”: {...},         # 原始 AI JSON
    “outcome”: null,            # 次日复盘后填入 correct/wrong
    “tags”: [“修复”, “微盘”, “连板”]
  }

写入时机：
- `emotion_node.ai_judge()` 执行后 → 追加 type=emotion 记录
- `index_timing.ai_judge()` 执行后 → 追加 type=timing 记录
- `theme_mode.detect()` 执行后 → 追加 type=theme 记录
- `short_term.detect()` 执行后 → 追加 type=shortterm 记录

检索接口（新增 src/memory.py）：

  def search_mtm(query: str, k: int = 5, days: int = 30) -> list[str]:
      “””检索最近 days 天的中期记忆，返回相关摘要列表。”””
      # 1. 加载最近 N 天的 memory_log.jsonl
      # 2. 对 summary 字段做 BM25 关键词匹配
      # 3. 返回 top-k 摘要供 LLM 上下文使用

接入 _build_messages()（ai_assistant.py 改动）：

  def _build_messages(question: str, history: list | None) -> list:
      ctx_chunks = knowledge.search(question, k=5)        # 长期知识 RAG
      mtm_chunks = memory.search_mtm(question, k=3)      # 中期记忆 RAG（新增）
      context = “\n\n---\n\n”.join(ctx_chunks + mtm_chunks)
      messages = [{“role”: “system”, “content”: SYSTEM_PROMPT.format(context=context)}]
      ...

---

三、长期记忆（LTM）具体实现

数据来源：
- knowledge/屠龙表 - 复盘表.csv → 历史复盘总结（自动提取）
- 用户手动记录的交易案例（成功/失败）
- AI 自动总结的市场规律（每周定期生成）

写入方式（追加到 .knowledge_index/ 向量库）：

  # src/memory.py
  def save_ltm(text: str, tags: list[str] = None) -> None:
      “””将交易经验写入长期记忆向量库，供 RAG 检索。”””
      doc = {“content”: text, “tags”: tags or [], “date”: today()}
      knowledge.add_document(doc)   # 调用 knowledge.py 的增量索引接口

检索：已有的 knowledge.search() 自动覆盖，无需额外改动。

---

四、文件清单（新增/修改）

  src/memory.py               # 新增：分层记忆管理
  ├── search_mtm()            # 中期记忆检索
  ├── append_mtm()            # 写入中期记忆
  └── save_ltm()              # 写入长期记忆

  .data/memory_log.jsonl      # 新增：中期记忆日志（追加写入）
  src/ai_assistant.py         # 修改：_build_messages() 接入 MTM
  src/emotion_node.py         # 修改：ai_judge() 后 append_mtm()
  src/index_timing.py         # 修改：ai_judge() 后 append_mtm()

---

五、效果对比

| 维度 | 改造前 | 改造后 |
|------|--------|--------|
| 跨会话记忆 | 无，每次冷启动 | MTM 提供近 30 天分析记录 |
| 经验积累 | 固定 knowledge/ 文档 | LTM 随使用自动增长 |
| 个性化 | 无 | AI 知道”你倾向于在修复期建仓” |
| 策略反思 | 无 | 可问”上次连板行情我判断对了吗” |
| 实现成本 | — | 仅新增 ~100 行 src/memory.py |



稳定性
应对“缓存雪崩”和“缓存击穿”

现状：你的缓存策略（spot=60s等）有潜在风险。

优化建议：

缓存雪崩：大量缓存同一时间失效（比如很多键都设了60s），导致瞬间请求全打到数据源。解决：为每个键的TTL增加一个随机偏移量（如60s ± 10s）。

缓存击穿：一个热点Key（如“贵州茅台”的实时行情）失效瞬间，大量请求同时去拉取数据。解决：使用分布式锁，只让一个请求去更新缓存，其他请求等待或返回旧数据。

全链路监控与告警

现状：架构图中缺少监控模块。

优化建议：部署Prometheus + Grafana监控体系。

业务指标：API响应时间、缓存命中率（按功能区分）、LLM调用耗时和Token消耗。

系统指标：Redis内存使用率、FastAPI服务CPU/内存、数据源API的可用性。

告警规则：当缓存命中率骤降、LLM调用失败率飙升、数据源响应超时时，通过飞书/钉钉/邮件告警。

异步任务队列与重试机制

现状：像“后台预热任务”这种，看起来是同步执行的。

优化建议：引入Celery + Redis/RabbitMQ任务队列。将耗时操作（如批量拉取历史K线、生成AI评测报告、复杂的策略回测）丢给后台Worker异步执行。前端（Streamlit）只需轮询任务状态。同时，任务队列自带的重试机制能优雅地处理数据源临时不可用的情况。


模型效率优化
# 1. 线上与回测不一致

这是最高优先级问题。

线上指数择时使用 `qwen-plus`，而历史回测使用 `qwen-turbo`；并且两者输入的特征不同：

- 线上包含全市场平均股价与 MA10 状态；
- 回测没有完整复用该特征；
- 成交额、历史上下文的构造也不完全一致。

当前择时结果：

| 指标 | 结果 |
|---|---:|
| 信号准确率 | 67.4%（58/86） |
| 周期准确率 | 54.7%（47/86） |
| 平均准确率 | 61.0% |

这说明现有分数不能完全代表线上生产判断的质量。

### 改进

建立“**线上同构回放**”：

```text
统一 Prompt 模板
统一模型与参数
统一特征构建函数
统一 Schema
统一硬规则
      ↓
线上：FeatureProvider 读取实时数据
回测：FeatureProvider 读取指定日期的历史数据
```

线上和回测只允许数据来源不同，不允许决策逻辑、prompt、模型版本不同。

2. 当前是 JSON 解析，不是严格的结构化决策

当前多个模块的处理方式是：Prompt 要求 JSON → 正则提取 `{...}` → `json.loads()` → 使用结果。

问题包括：

- `signal` 未严格限制为 `多/空/转/观望`；
- 周期未严格限制为 `趋势A/龙头A/B/C/D`；
- 情绪节点未完全校验是否属于节点全集；
- 个股候选未校验是否来自输入候选池；
- 仓位建议未由程序重新计算；
- 模型理由可能引用输入中不存在的“证据”。

### 改进

为每类高风险决策定义严格 Schema，例如指数择时：

```json
{
  "signal": "多 | 空 | 转 | 观望",
  "mid_cycle": "趋势A | 龙头A | B | C | D",
  "confidence": 0.0,
  "abstain": false,
  "evidence": [
    {
      "feature": "avg_price_vs_ma10",
      "value": "below",
      "direction": "bearish"
    }
  ],
  "invalidation": ["平均股价重新站上 MA10 且成交额放大"],
  "position_cap": 0.3
}
```

然后增加业务校验器：

1. JSON Schema / Pydantic 校验；
2. 枚举值校验；
3. 置信度范围校验；
4. 必填证据校验；
5. `evidence` 必须对应输入特征；
6. 仓位上限由程序重算；
7. 输出失败时允许一次修复重试；
8. 再失败则返回“观望/数据不足”，不能生成伪确定结论。

3. 情绪节点的评测标签与覆盖存在问题

已观察到的结果中，情绪节点曾出现：

- 覆盖：103/424；
- 准确率：约 30.1%；
- 最近一次评测又出现 0/100 匹配，因为评测截取数据集前 100 条，但预测缓存集中在近期日期。

此外，历史标签包含“癫狂”“反弹第一天”“反弹加速”等，而生产节点集合未必完整覆盖这些标签。

### 改进

1. 建立完整的 `raw_label -> canonical_label` 标签映射。
2. 明确一级分类与二级节点：
   - 一级：主升、修复、混沌、退潮、冰点等；
   - 二级：修复弱/中/强、主升确认/加速/高潮等。
3. 评测按日期交集筛选，不再使用固定 `[:100]`。
4. 同时报告：
   - 覆盖率；
   - 一级 Macro-F1；
   - 二级 Macro-F1；
   - 混淆矩阵；
   - 高风险错误率；
   - 弃权比例。
5. 先稳定“退潮/非退潮”“修复/非修复”“主升/非主升”等一级判断，再细分节点。

在准确率明显提升之前，情绪节点应定位为**辅助观察指标**，不宜单独作为进攻信号。

模型仍承担了过多确定性判断

短线起变等任务已经有大量可程序计算的条件：

- 连板空间；
- 是否一字板；
- 炸板次数；
- 连板指数形态；
- 微盘指数是否安全；
- 候选是否来自连板天梯。

这类事实和门槛应由程序判断；模型只处理规则无法覆盖的边界语义、题材关联、候选排序和可读解释。

### 改进：三层决策

| 层级 | 责任 | 示例 |
|---|---|---|
| 硬门控 | 程序 | 数据是否新鲜、D 周期、仓位上限、一字板排除、候选池合法性 |
| 特征评分 | 程序 | 涨停/跌停、大面变化、量能、连板溢价、板块扩散 |
| LLM 裁决 | 模型 | 边界案例、多个候选排序、解释、交易计划 |

模型不要替代程序判定事实；模型应该解释证据与处理模糊边界。

---

模型使用策略
使用双阶段复核

```text
数据与特征
  ↓
硬规则门控
  ↓
qwen-plus：结构化初判 + 证据 + 置信度
  ↓
Schema/业务规则校验
  ↓
是否低置信、高风险、前后跳变或冲突？
  ├─ 否：输出观察/仓位上限
  └─ 是：qwen3.7-max 对抗式复核
             ↓
        一致：输出
        分歧：观望、降仓、等待确认
```

复核模型不应该只阅读第一模型的自然语言结论，而应根据**同一份结构化特征**独立判断，并重点回答：

1. 是否违反硬规则？
2. 哪一条证据不足？
3. 有哪些反例会推翻结论？
4. 是否应弃权？
5. 需要哪一个确认条件才允许升级仓位？





评测升级

模型成功率应拆为三层。

## 第一层：输出可靠性

必须接近 100%。

- JSON/schema 合法率；
- 枚举合法率；
- 工具参数合法率；
- 证据可追溯率；
- 硬规则违规率；
- 数据不新鲜时的弃权正确率。

## 第二层：标签一致性

- 择时：信号准确率、周期准确率；
- 情绪：一级/二级 Macro-F1、混淆矩阵；
- 主线：板块识别 Precision / Recall；
- 短线：起变信号 Precision / Recall；
- 个股：候选合法率、买卖条件合规率。

不要只看总准确率，要关注危险类别的误判。例如“退潮误判为主升”的损失远大于“修复中等误判为修复弱”。

## 第三层：真实交易质量

为每一条预测持久化：

```text
prediction_id
as_of_timestamp
data_cutoff_timestamp
model_id
prompt_version
feature_version
signal / node / decision
confidence
evidence_snapshot
position_cap
future_outcome
```

然后按任务评估：

| 任务 | 建议指标 |
|---|---|
| 多/空/转 | 次日、3 日、5 日方向命中率 |
| 仓位建议 | 风险调整收益、最大回撤、胜率 |
| 情绪节点 | 节点后 1/3/5 日连板溢价、负反馈风险 |
| 主线 | 板块未来 N 日超额收益、成分股扩散度 |
| 短线起变 | 信号后连板指数收益、候选池收益、失败率 |
| 个股判断 | MFE、MAE、止损触发率、相对基准超额收益 |

## 防止未来信息泄漏

回测日期为 T 时：

1. 只读取 T 的盘中或收盘时点可取得的数据；
2. 不读取 T 之后的 CSV 标签、题材归属或最终复盘结论；
3. 不把同日复盘标签混入模型输入后又拿它做标准答案；
4. 线上和回测的特征、prompt、规则、模型版本必须一致；
5. 数据、特征、prompt、模型均要有版本记录。

### 数据正确性
| # | 问题 | 位置 | 修改 |
|---|------|------|------|
| 1 | 拉取失败股票用"昨价拉平"伪造 390 日 OHLCV，污染平均股价/MA/百日新高/情绪统计 | data.py:946-980 | 删除伪造逻辑，失败样本剔除 |
| 2 | 确定性未来函数：历史平均股价取缓存末端 n 天，非目标日之前 | feature_provider.py:106-120 | 按 `dates<=target` 截断，非交易日向前找 |
| 3 | 指数 Parquet 覆盖写：短 days 请求可把 1060 天历史截成 3 天 | ts_store.py:47-55 | 按日期 merge+原子写+文件锁 |
| 4 | 缓存预热 key 与业务读不一致（days=1060 vs 380），预热白做 | server.py:145-151 vs data.py:515 | 参数归一化，预热写全量、读时切片 |
| 5 | Redis 锁无 token，过期后可误删他人锁 | redis_cache.py:165-191 | UUID token+Lua compare-and-delete |
| 6 | 失败结果也缓存 60s：一次源故障→全市场空数据 | redis_cache.py:193-197 | 失败不缓存或 5s 负缓存 |
| 7 | 复权口径混用：AkShare 前复权 vs Fuyao 不复权，不标注 | data.py:217-224 | 口径不一致时拒绝或显式标注 |
| 8 | 异步无限流：全市场 ~65 请求一次 gather，无 429/退避 | async_fetch.py:73-84 | 每源 Semaphore+限流+熔断 |


策略逻辑
| # | 问题 | 位置 | 修改 |
|---|------|------|------|
| 9 | 起变门控漏"高度板非一字板"（规范明确要求） | short_term.py:495-498 | is_signal 加入该条件 |
| 10 | D 周期空仓可绕过："D+多"时仓位可达 1.0 | index_timing.py:147-153 | D 周期程序级强制 cap=0 |
| 11 | 个股 D 周期禁令只在 Prompt，代码仍执行 | single_stock.py:443-482 | 程序级门控直接返回空 |
| 12 | `bool("false")==True` 反向触发弃权；JSON 贪婪正则提取 | schema_validator.py:62-86,213 | Pydantic+API JSON mode |
| 13 | 规范文档声明了但运行时未读入，规则硬编码 Prompt，文档实现漂移 | index_timing.py:24 等 4 处 | 运行时读规范作为单一事实来源 |
| 14 | 延续信号 premium_pct 未返回，文案永远"0%溢价" | short_term.py:634-641 | gate 返回该字段 |
| 15 | 所有 LLM 调用无 timeout，盘中可能永久阻塞 | 各模块 chat 调用 | 统一 LLM 网关：timeout/重试/限流/token 统计 |


安全
| # | 问题 | 位置 | 修改 |
|---|------|------|------|
| 16 | 8601/8602 绑 0.0.0.0 无登录无限流，任何人可触发 rejudge 烧 API Key | run.sh、server.py | 监听 127.0.0.1+反代+登录；高成本接口鉴权配额 |
| 17 | CORS `["*"]` + Streamlit 关闭 XSRF | server.py:83-88、config.toml | 白名单+开启 XSRF |
| 18 | 分组写共享 JSON 无锁；UI 多处 unsafe HTML 拼用户/AI 输入 | stock_filter.py:535、ui.py | 文件锁原子写；html.escape |



数据层

1数据源太“野鸡”，全是免费轮询
你的现状：依赖腾讯/新浪/东财的免费HTTP接口，30秒轮询一次，没有Level2，没有Tick数据，分钟线也缺失。付费的Fuyao API只用来抓热榜，大材小用了。

优化方案：分级数据架构 + 专业数据源补位

数据分级：不要试图让一个数据源干所有事。把数据分成三层：

L1（基础层）：继续用免费API，但仅用于盘后非实时数据，比如日K线、股票基础信息。这些数据对时效性要求低，免费够用。

L2（专业层）：将Fuyao API从“只拿热榜”升级为实时行情主力。它支持WebSocket推送，可以获取Level1/Level2数据。哪怕只订阅几十只自选股，也比你轮询3000只股票要精确得多，而且能拿到逐笔成交（Tick）、盘口（Order Book）等真正有用的微观数据。

L3（维护层）：用akshare作为“最后的兜底”，只在L1和L2都失败时调用，确保系统不崩溃。

补上分钟线：分钟线是技术分析的基础。你可以：

通过Fuyao API的WebSocket，实时聚合TICK数据生成1分钟K线。

或使用akshare的stock_zh_a_hist_min_em接口，在盘后批量补拉历史分钟线，存入你的DuckDB时序库。

预期效果：数据延迟从轮询的“30秒”降到“毫秒级”，并且真正有了“微观结构”数据。



痛点2：无交易日历，回测全是“幸存者偏差”
你的现状：只排了周末，遇上国庆、春节长假就全错。最致命的是回测时，用现在的股票列表去测过去的数据——那些已经退市的、ST的、停牌的股票，因为现在不在列表里，就被自动忽略了，这会严重高估策略收益。

优化方案：建立“时点化”的证券主数据

交易日历：
使用同花顺金融数据API里的A股交易日历
交易日历模块，提供 A 股近一年交易日序列。 返回统一响应信封 ApiResponse，业务错误经 code 字段表达，HTTP 状态码恒为 200。

通用约定
Base URL：https://fuyao.aicubes.cn。
必需请求头：X-api-key，缺失或无效返回 code=2001。
信封 data.timestamp 为毫秒级 Unix 时间戳；item 内同时返回毫秒戳 date_ms 与可读日期 date（yyyyMMdd），时区按 Asia/Shanghai。



证券主数据时点化（解决幸存者偏差）：

核心思想：回测某一天时，使用的股票池必须是那一天真实存在的股票。

具体做法：用akshare的stock_info_a_code_name()（当前列表）和历史退市列表拼接，或者用stock_zh_a_spot（需要历史快照）。更简单的办法：每天晚上，把当天的全市场股票列表（代码、名称、上市日期、是否ST）作为一个“快照”存入你的时序库。回测时，根据回测日期，取该日期之前的最近一个快照作为股票池。

维护字段：至少包含stock_code、stock_name、list_date（上市日）、delist_date（退市日，未退市则为空）、st_status（是否为ST）。

预期效果：回测结果不再是“美颜”过的，更贴近真实交易环境。


痛点3：WebSocket是“假”的，UI整页刷新
你的现状：FastAPI后端的/ws/market，本质上还是用轮询去拉数据，然后推给前端，只是把“轮询”这个动作从浏览器搬到了服务器。Streamlit前端更是粗暴地整页st.rerun。

优化方案：真WebSocket推送 + 前端增量更新


后端改造（真WS）：

后台启动一个独立的协程，通过Fuyao API的WebSocket接收行情推送。

收到推送后，立即更新Redis中的实时价格、成交量等关键字段。

同时，通过FastAPI的/ws/quotes/{code}，只将变化的数据（增量）广播给订阅了该股票的所有前端客户端。

前端改造（增量更新）：

Streamlit本身不擅长WebSocket，但你可以混血开发：

在Streamlit页面中嵌入一个轻量级的HTML/JS组件（用st.components.v1.html），这个组件专门负责维护WebSocket连接。

JS组件收到数据后，通过Streamlit.setComponentValue把最新价格“喂”给Streamlit的Python变量。

Python端只刷新价格、涨跌幅这些数值组件，而用Plotly画的K线图等大图表，只在用户操作或定时（如每30秒）时才刷新。

或者：直接放弃Streamlit，前端转向Vue/React，通过WebSocket与FastAPI后端通信，这才是终局方案。

预期效果：带宽和CPU消耗下降90%，UI响应丝滑，不再是“整页闪动”。






痛点4：数据质量无校验，无熔断
你的现状：数据拿到后直接用，如果API返回了错误数据（比如价格为0，或涨跌幅异常），系统不会感知，可能导致AI给出错误建议。

优化方案：构建数据质量“看门狗”

数据校验（入库前）：

字段完整性：检查返回的JSON里，price、volume等关键字段是否存在且不为空。

OHLC约束：验证high >= max(open, close)和low <= min(open, close)。如果违反，标记为“异常数据”，丢弃并告警。

阈值过滤：如果某只股票价格变动超过±20%（且非新股），大概率是数据错误或除权未复权，也应丢弃。

熔断与降级：

重试机制：如果腾讯API连续失败3次，立刻切换到新浪API，如果还失败，再用Fuyao API。

降级返回：如果所有数据源都挂了，不要给前端返回空或错误。从Redis中返回最近一次成功缓存的数据，并明确标记data_source="cache"和stale=True。这样UI仍然有数据显示，只是不那么新鲜，用户体验会好很多。

健康检查：为每个数据源建立一个“健康评分”。如果某源失败率超过阈值（如20%），自动将其加入黑名单，休息5分钟后再试。

预期效果：系统从“一碰就碎”变成“坚韧可靠”，即使外部数据源出问题，系统也能优雅降级，不至于完全不可用。











